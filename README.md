# NBA Player Points Projection Tool

This project is an interactive AI/ML decision tool that predicts an NBA player's next-season total points using historical NBA performance data, advanced statistics, and team context.

## How to Run

1. Install the required packages:

```bash
py -m pip install -r requirements.txt